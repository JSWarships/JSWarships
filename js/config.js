/*const fs = require('fs');

class ConfigManager {
  static getConfig() {
    const config = fs.readFile('config.json', 'utf8', (err, data) => {
      console.log(data);
    });
    return config;
  }
}

module.exports = ConfigManager;*/
